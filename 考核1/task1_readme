考核一：
本次考核才用DMA+空闲中断+不定长数据收发。具体内容在回调函数上。

对该代码的理解：该代码比较简洁，用非轮询方式（DMA）解决了数据的不定长收发。
                但缺点很明显：在小量数据收发的能应付过来，但当数据过大时，程序在回调函数的时间会边长，对程序影响很大。
                如果确实出现大量数据需要处理时，可以采用利用缓冲区，在主程序while里面进行收发，在这里并没有写。仅仅作为我对代码的理解


（可忽略：改代码在回调函数中额外添加了：输入通信格式使小灯泡亮起的操作，不属于本次考核任务的内容，可以忽略）
